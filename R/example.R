# Write your R functions here
# Each .R file in /R should contain functions with their helpers 
# Functions with different uses should be put into separate files
# Use roxygen2 for inline documentation
example_func <- function(x,y) {
  x * y
}
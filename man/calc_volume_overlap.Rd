% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/volume.R
\name{calc_volume_overlap}
\alias{calc_volume_overlap}
\alias{calc_volume_overlap,SpatEnvelope,SpatEnvelope-method}
\alias{calc_volume_overlap,SpatVoxel,SpatVoxel-method}
\alias{calc_volume_overlap,SpatEnvelope,SpatVoxel-method}
\alias{calc_volume_overlap,SpatVoxel,SpatEnvelope-method}
\alias{calc_volume_overlap,SpatRaster,SpatRaster-method}
\alias{calc_volume_overlap,SpatRaster,SpatVolume-method}
\alias{calc_volume_overlap,SpatVolume,SpatRaster-method}
\title{Per-cell 3D volume overlap between two rasterized domains}
\usage{
calc_volume_overlap(x, y, ...)

\S4method{calc_volume_overlap}{SpatEnvelope,SpatEnvelope}(x, y, ...)

\S4method{calc_volume_overlap}{SpatVoxel,SpatVoxel}(
  x,
  y,
  bounds = c("top", "midpoint"),
  fun = function(v) !is.na(v)
)

\S4method{calc_volume_overlap}{SpatEnvelope,SpatVoxel}(x, y, bounds = c("top", "midpoint"), ...)

\S4method{calc_volume_overlap}{SpatVoxel,SpatEnvelope}(x, y, bounds = c("top", "midpoint"), ...)

\S4method{calc_volume_overlap}{SpatRaster,SpatRaster}(x, y, ...)

\S4method{calc_volume_overlap}{SpatRaster,SpatVolume}(x, y, ...)

\S4method{calc_volume_overlap}{SpatVolume,SpatRaster}(x, y, ...)
}
\arguments{
\item{x, y}{The two domains, each a \link[=SpatEnvelope-class]{SpatEnvelope} or
\link[=SpatVoxel-class]{SpatVoxel}.}

\item{...}{Arguments for the voxel methods, which are an error for a pair
of envelopes.}

\item{bounds}{Voxel methods only. See \code{\link[=volume]{volume()}}. Also governs how an
envelope is discretized when the two inputs differ.}

\item{fun}{Voxel methods only. See \code{\link[=volume]{volume()}}.}
}
\value{
Multi-layer SpatRaster with 9 layers:
\describe{
\item{depth_min_a, depth_max_a}{Depth limits of \code{x} (m)}
\item{depth_min_b, depth_max_b}{Depth limits of \code{y} (m)}
\item{depth_min_overlap, depth_max_overlap}{Depth limits of the
intersection (m). NA where the domains do not overlap in depth.}
\item{volume_a, volume_b}{Per-cell volume of each domain (km³)}
\item{volume_overlap}{Per-cell overlap volume (km³). \code{0} where both are
present but their depths do not intersect, NA where either is
absent.}
}

For voxel inputs the depth layers are \emph{summary bounds} — the shallowest
and deepest occupied level. A voxel with an interior gap is not solid
between them, so read the volume layers, not the depth layers, for
occupied volume. The result is a plain SpatRaster: it is neither an
envelope nor a voxel.
}
\description{
Computes the depth interval and volume of each domain and of their
intersection, cell by cell. Accepts any combination of
\link[=SpatEnvelope-class]{SpatEnvelope} and \link[=SpatVoxel-class]{SpatVoxel}; when the two differ, the
envelope is discretized onto the voxel's depth levels with
\code{\link[=envelope_to_voxel]{envelope_to_voxel()}} rather than the voxel being collapsed, because
\code{\link[=voxel_to_envelope]{voxel_to_envelope()}} fills interior gaps and would overstate the overlap.
}
\details{
Two voxels must be sampled at the same depth levels, and all inputs must be
on the same grid.
}
\examples{
fp <- terra::rast(nrows = 1, ncols = 2, xmin = 0, xmax = 2000,
                  ymin = 0, ymax = 1000,
                  crs = "+proj=laea +lat_0=0 +lon_0=0 +datum=WGS84 +units=m")
terra::values(fp) <- c(1, 1)

a <- as_envelope(fp, depth_min = 0, depth_max = 100)
b <- as_envelope(fp, depth_min = 50, depth_max = 200)

ov <- calc_volume_overlap(a, b)
names(ov)
terra::global(ov[[c("volume_a", "volume_b", "volume_overlap")]], "sum",
              na.rm = TRUE)

# Mixed input: the envelope is discretized onto the voxel's levels.
bv <- envelope_to_voxel(b, depths = c(0, 50, 100, 150, 200))
terra::global(calc_volume_overlap(a, bv)[["volume_overlap"]], "sum",
              na.rm = TRUE)
}
\seealso{
\code{\link[=count_3d_overlap]{count_3d_overlap()}} when only the presence of overlap is needed.
}

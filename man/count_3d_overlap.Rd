% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/volume.R
\name{count_3d_overlap}
\alias{count_3d_overlap}
\alias{count_3d_overlap,SpatEnvelope,SpatEnvelope-method}
\alias{count_3d_overlap,SpatVoxel,SpatVoxel-method}
\alias{count_3d_overlap,SpatEnvelope,SpatVoxel-method}
\alias{count_3d_overlap,SpatVoxel,SpatEnvelope-method}
\alias{count_3d_overlap,SpatRaster,SpatRaster-method}
\alias{count_3d_overlap,SpatRaster,SpatVolume-method}
\alias{count_3d_overlap,SpatVolume,SpatRaster-method}
\title{Binary 3D overlap between two rasterized domains}
\usage{
count_3d_overlap(x, y, ...)

\S4method{count_3d_overlap}{SpatEnvelope,SpatEnvelope}(x, y, ...)

\S4method{count_3d_overlap}{SpatVoxel,SpatVoxel}(x, y, fun = function(v) !is.na(v))

\S4method{count_3d_overlap}{SpatEnvelope,SpatVoxel}(x, y, bounds = c("top", "midpoint"), ...)

\S4method{count_3d_overlap}{SpatVoxel,SpatEnvelope}(x, y, bounds = c("top", "midpoint"), ...)

\S4method{count_3d_overlap}{SpatRaster,SpatRaster}(x, y, ...)

\S4method{count_3d_overlap}{SpatRaster,SpatVolume}(x, y, ...)

\S4method{count_3d_overlap}{SpatVolume,SpatRaster}(x, y, ...)
}
\arguments{
\item{x, y}{The two domains, each a \link[=SpatEnvelope-class]{SpatEnvelope} or
\link[=SpatVoxel-class]{SpatVoxel}.}

\item{...}{Arguments for the voxel methods, which are an error for a pair
of envelopes.}

\item{fun}{Voxel methods only. See \code{\link[=volume]{volume()}}.}

\item{bounds}{Used only where an envelope must be discretized onto a
voxel's levels. See \code{\link[=volume]{volume()}}.}
}
\value{
Single-layer SpatRaster named \code{overlap}, \code{1} where the two domains
overlap in 3D and \code{NA} elsewhere.
}
\description{
Returns a single-layer raster that is \code{1} in cells where the two domains
overlap both horizontally (both present) and vertically (their depths
intersect), and \code{NA} otherwise. Use it for richness and tally maps, where
the per-cell overlap volume is not needed: unlike \code{\link[=calc_volume_overlap]{calc_volume_overlap()}}
it computes only the presence pattern, with no cell areas and no volumes.
}
\details{
Mixed input is resolved the same way as in \code{\link[=calc_volume_overlap]{calc_volume_overlap()}} — the
envelope is discretized onto the voxel's depth levels.
}
\examples{
fp <- terra::rast(nrows = 1, ncols = 3, xmin = 0, xmax = 3000,
                  ymin = 0, ymax = 1000,
                  crs = "+proj=laea +lat_0=0 +lon_0=0 +datum=WGS84 +units=m")
terra::values(fp) <- c(1, 1, NA)

a <- as_envelope(fp, depth_min = 0, depth_max = 100)
b <- as_envelope(
  fp, depth_min = 0,
  depth_max = terra::setValues(terra::rast(fp), c(50, NA, 300))
)

# Cell 1 overlaps; cell 2 has no B; cell 3 has no A.
terra::values(count_3d_overlap(a, b))

# Summing a stack of these gives a richness map.
terra::global(count_3d_overlap(a, b), "sum", na.rm = TRUE)
}
\seealso{
\code{\link[=calc_volume_overlap]{calc_volume_overlap()}} for the overlap volume itself.
}

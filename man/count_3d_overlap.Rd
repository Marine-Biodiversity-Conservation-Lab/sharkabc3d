% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volume.R
\name{count_3d_overlap}
\alias{count_3d_overlap}
\title{Binary 3D overlap between two rasterized ranges}
\usage{
count_3d_overlap(range_rast_a, range_rast_b)
}
\arguments{
\item{range_rast_a}{SpatRaster. First rasterized range (output of
\code{\link[=rasterize_range]{rasterize_range()}}).}

\item{range_rast_b}{SpatRaster. Second rasterized range (output of
\code{\link[=rasterize_range]{rasterize_range()}}).}
}
\value{
Single-layer SpatRaster (\code{1} where the two ranges overlap in 3D,
\code{NA} elsewhere).
}
\description{
Returns a single-layer raster that is \code{1} in cells where the two ranges
overlap both horizontally (both present) and vertically (their depth
intervals intersect), and \code{NA} otherwise. Useful for richness / tally
maps where the per-cell overlap volume is not needed.
}

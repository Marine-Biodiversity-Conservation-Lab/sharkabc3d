% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_3d_objects.R
\name{create_study_voxel}
\alias{create_study_voxel}
\title{Create a study voxel: the 3D voxel grid space for an analysis}
\usage{
create_study_voxel(template, bathymetry, depths)
}
\arguments{
\item{template}{SpatRaster. Empty raster defining the horizontal grid
(extent, resolution, CRS) whose cells become the footprint of each voxel
column, e.g. from \code{\link[=create_study_raster]{create_study_raster()}}.}

\item{bathymetry}{SpatRaster. GEBCO-style elevation raster with negative
values below sea level (e.g. from \code{\link[=load_gebco_bathymetry]{load_gebco_bathymetry()}}). It is projected
onto \code{template} and flipped to positive seafloor depth.}

\item{depths}{Numeric vector. Standard depth levels in metres (e.g. the
World Ocean Atlas standard depths) that set the vertical resolution of the
voxel model.}
}
\value{
A \code{study_voxel} object: a list with \code{grid} (the empty horizontal
template), \code{seafloor} (positive seafloor depth on that grid), and \code{depths}
(sorted standard depth levels).
}
\description{
Bundle the three things every 3D operation in the package needs into one
\code{study_voxel} object: a horizontal grid template, the seafloor depth on that
grid, and the standard depth levels that define the vertical resolution of
the voxel model. The supplied bathymetry is projected onto the template and
converted to a positive seafloor depth (land clamped to 0), so callers no
longer prepare the seafloor by hand.
}

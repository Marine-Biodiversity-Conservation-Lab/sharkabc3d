% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_rast_range}
\alias{extract_rast_range}
\title{Mask a 3D raster by a rasterized species range}
\usage{
extract_rast_range(range_rast, rast_3d)
}
\arguments{
\item{range_rast}{SpatRaster. Output of \code{\link[=rasterize_range]{rasterize_range()}} with layers
\code{depth_min} and \code{depth_max} in metres.}

\item{rast_3d}{SpatRaster. Multi-depth raster with layer names following
the \verb{\{variable\}_depth=\{value\}} convention.}
}
\value{
SpatRaster with the same layers as \code{rast_3d}, masked to the
per-cell depth window of \code{range_rast}.
}
\description{
Given a multi-depth environmental raster (\code{rast_3d}) and a rasterized range
(\code{range_rast}, the output of \code{\link[=rasterize_range]{rasterize_range()}} with per-cell \code{depth_min}
and \code{depth_max} layers clamped to bathymetry), return a 3D raster where
each cell retains the environmental value only at depths inside that
cell's \verb{[depth_min, depth_max]} window. Cells outside the range are NA
across all depth layers.
}
\details{
This preserves per-cell vertical refuge — e.g., a species whose maximum
depth is 1000 m but the seafloor is 400 m will only "see" the 0-400 m
layers at that cell. Used as the underlying extractor for species-range
depth profiles and range-aware environmental summaries.

\code{range_rast} and \code{rast_3d} must share extent, resolution, and CRS. The
caller is responsible for alignment (typically by rasterizing onto a grid
derived from \code{rast_3d}, or by pre-projecting with
\code{terra::project(range_rast, rast_3d[[1]], method = "near")}).
}

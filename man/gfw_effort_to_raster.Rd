% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gfw.R
\name{gfw_effort_to_raster}
\alias{gfw_effort_to_raster}
\title{Rasterise a GFW effort tibble onto a target grid}
\usage{
gfw_effort_to_raster(
  effort,
  grid,
  layer_by = "geartype",
  value = "Apparent Fishing Hours",
  fun = "sum"
)
}
\arguments{
\item{effort}{Data frame. Output of \code{gfwr::gfw_ais_fishing_hours()} (a
long-format tibble with at minimum \code{Lat}, \code{Lon}, a value, and a
grouping column).}

\item{grid}{SpatRaster. Target grid (extent, resolution, CRS) — typically
the same grid used for species ranges and WOA extraction.}

\item{layer_by}{Character. Column in \code{effort} whose levels become layers.
\code{NULL} produces a single-layer total-effort raster. Default
\code{"geartype"}.}

\item{value}{Character. Column in \code{effort} to aggregate. Default
\code{"Apparent Fishing Hours"}.}

\item{fun}{Character or function. Aggregation applied to records that
fall into the same cell × layer level. Default \code{"sum"}.}
}
\value{
A SpatRaster with one layer per \code{layer_by} level (or one layer
if \code{layer_by = NULL}). Layer names follow \verb{effort_<level>}.
}
\description{
Turn the long-format apparent-fishing-hours tibble returned by
\code{gfwr::gfw_ais_fishing_hours()} (formerly \code{gfwr::get_raster()}) into a
multi-layer SpatRaster on the package's canonical study grid. Each
level of \code{layer_by} becomes its own layer, named
\verb{effort_<level>} (e.g. \code{effort_drifting_longlines}).
}
\details{
The input is expected to carry a cell centroid (\code{Lat}, \code{Lon}), a value
column (default \code{"Apparent Fishing Hours"}), and one categorical column
matching the API's \code{group_by} — for example \code{geartype} or \code{flag} (note
lower-case; this is what \code{gfwr} actually returns). Records that fall
into the same target cell × layer level are aggregated with \code{fun}.
}

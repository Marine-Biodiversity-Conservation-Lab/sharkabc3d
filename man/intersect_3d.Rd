% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/intersect.R
\name{intersect_3d}
\alias{intersect_3d}
\alias{intersect_3d,SpatEnvelope,SpatEnvelope-method}
\alias{intersect_3d,SpatVoxel,SpatVoxel-method}
\alias{intersect_3d,SpatEnvelope,SpatVoxel-method}
\alias{intersect_3d,SpatVoxel,SpatEnvelope-method}
\alias{intersect_3d,SpatEnvelope,ANY-method}
\alias{intersect_3d,SpatVoxel,ANY-method}
\alias{intersect_3d,ANY,SpatEnvelope-method}
\alias{intersect_3d,ANY,SpatVoxel-method}
\alias{intersect_3d,ANY,ANY-method}
\alias{intersect-guard}
\alias{intersect,SpatEnvelope,SpatEnvelope-method}
\alias{intersect,SpatEnvelope,SpatVoxel-method}
\alias{intersect,SpatVoxel,SpatEnvelope-method}
\alias{intersect,SpatVoxel,SpatVoxel-method}
\alias{intersect,SpatEnvelope,SpatRaster-method}
\alias{intersect,SpatVoxel,SpatRaster-method}
\alias{intersect,SpatRaster,SpatEnvelope-method}
\alias{intersect,SpatRaster,SpatVoxel-method}
\title{The 3D space two objects share}
\usage{
intersect_3d(x, y, ...)

\S4method{intersect_3d}{SpatEnvelope,SpatEnvelope}(x, y, ...)

\S4method{intersect_3d}{SpatVoxel,SpatVoxel}(x, y)

\S4method{intersect_3d}{SpatEnvelope,SpatVoxel}(x, y, bounds = c("top", "midpoint"), ...)

\S4method{intersect_3d}{SpatVoxel,SpatEnvelope}(x, y, bounds = c("top", "midpoint"), ...)

\S4method{intersect_3d}{SpatEnvelope,ANY}(x, y, ...)

\S4method{intersect_3d}{SpatVoxel,ANY}(x, y)

\S4method{intersect_3d}{ANY,SpatEnvelope}(x, y, ...)

\S4method{intersect_3d}{ANY,SpatVoxel}(x, y, ...)

\S4method{intersect_3d}{ANY,ANY}(x, y, ...)
}
\arguments{
\item{x, y}{The two objects. At least one must be a \link[=SpatEnvelope-class]{SpatEnvelope} or
a \link[=SpatVoxel-class]{SpatVoxel}. The other can be either of those, a single-layer
\code{SpatRaster} footprint, or polygons (\code{SpatVector}, \code{sf}, \code{sfc}). Rasters
must share one grid (CRS, extent, resolution).}

\item{...}{Arguments for the voxel methods. They are an error when both
inputs are envelopes.}

\item{bounds}{Only when one input is an envelope and the other a voxel. How
the envelope is placed on the voxel's depth levels: \code{"top"} (default) or
\code{"midpoint"}. See \code{\link[=envelope_to_voxel]{envelope_to_voxel()}}.}
}
\value{
A \link[=SpatEnvelope-class]{SpatEnvelope} when both inputs are envelopes, or when one
is an envelope and the other is 2D. Otherwise a \link[=SpatVoxel-class]{SpatVoxel} of
presence. Either is on the grid of the 3D input.
}
\description{
\code{intersect_3d()} returns the part of space that is inside both \code{x} and
\code{y}. It answers the question "what do these two share?". To ask only
"do they share anything?", use \code{\link[=intersects_3d]{intersects_3d()}}. To keep one object's
values where the other is present, use \code{\link[terra:mask]{mask()}}.
}
\details{
At least one of \code{x} and \code{y} must be a 3D object: a \link[=SpatEnvelope-class]{SpatEnvelope} or
a \link[=SpatVoxel-class]{SpatVoxel}. The other can be a 3D object too, or a 2D object. A 2D
object restricts the result horizontally and leaves its depths alone.

\describe{
\item{Two envelopes}{Cell by cell, the result runs from the deeper
\code{depth_min} to the shallower \code{depth_max}. Intervals that only touch
share no water, so that cell is empty (\code{NA}).}
\item{Two voxels}{Both must be sampled at the same depth levels. A level
is in the result where both voxels occupy it.}
\item{An envelope and a voxel}{The envelope is first placed on the
voxel's depth levels with \code{\link[=envelope_to_voxel]{envelope_to_voxel()}}. The result is a voxel.}
\item{A 3D object and a \code{SpatRaster}}{The raster is a footprint. Its
non-\code{NA} cells are inside; its values are not read. It must have one
layer and be on the same grid.}
\item{A 3D object and polygons}{A \code{SpatVector}, \code{sf} or \code{sfc} object. The
polygons are rasterised onto the 3D object's grid. A cell is inside when
its centre is. Polygons in another CRS are projected first.}
}

The order of \code{x} and \code{y} does not matter.

The result is a domain, not a field. An envelope result carries the shared
depth interval. A voxel result carries presence: \code{1} where a level is
shared, \code{NA} elsewhere, in layers named \verb{presence_depth=<value>}. The cell
values of a voxel input are not carried over. To keep them, use \code{\link[terra:mask]{mask()}}.
}
\section{terra's \code{intersect()}}{

\code{terra::intersect()} on two rasters is a 2D test that ignores depth. On a
\link[=SpatEnvelope-class]{SpatEnvelope} or a \link[=SpatVoxel-class]{SpatVoxel}, in either position, it is an
error that points to \code{\link[=intersects_3d]{intersects_3d()}} and \code{intersect_3d()}. Plain
rasters, vectors and extents keep terra's behaviour.
}

\examples{
fp <- terra::rast(nrows = 1, ncols = 3, xmin = 0, xmax = 3000,
                  ymin = 0, ymax = 1000,
                  crs = "+proj=laea +lat_0=0 +lon_0=0 +datum=WGS84 +units=m")
terra::values(fp) <- c(1, 1, 1)

# Two species: one at 0-100 m, one at 50-200 m everywhere.
a <- as_envelope(fp, depth_min = 0, depth_max = 100)
b <- as_envelope(fp, depth_min = 50, depth_max = 200)

# They share 50-100 m in every cell.
shared <- intersect_3d(a, b)
terra::values(shared)
volume(shared)

# With a voxel, the result is a presence voxel on the voxel's levels.
bv <- envelope_to_voxel(b, depths = c(0, 50, 100, 150, 200))
names(intersect_3d(a, bv))

# With polygons, the envelope is kept where the polygon is.
poly <- terra::vect("POLYGON ((0 0, 2000 0, 2000 1000, 0 1000, 0 0))",
                    crs = terra::crs(fp))
terra::values(intersect_3d(a, poly))
}
\seealso{
\code{\link[=intersects_3d]{intersects_3d()}} for the yes/no form; \code{\link[terra:mask]{mask()}} to keep a voxel's
values inside a domain; \code{\link[=volume]{volume()}} for the volume of the result;
\code{\link[=calc_volume_overlap]{calc_volume_overlap()}}, which is built on this function.
}

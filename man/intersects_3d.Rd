% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/intersect.R
\name{intersects_3d}
\alias{intersects_3d}
\alias{intersects_3d,SpatEnvelope,SpatEnvelope-method}
\alias{intersects_3d,SpatVoxel,SpatVoxel-method}
\alias{intersects_3d,SpatEnvelope,SpatVoxel-method}
\alias{intersects_3d,SpatVoxel,SpatEnvelope-method}
\alias{intersects_3d,SpatEnvelope,ANY-method}
\alias{intersects_3d,SpatVoxel,ANY-method}
\alias{intersects_3d,ANY,SpatEnvelope-method}
\alias{intersects_3d,ANY,SpatVoxel-method}
\alias{intersects_3d,ANY,ANY-method}
\title{Do two objects share any 3D space?}
\usage{
intersects_3d(x, y, ...)

\S4method{intersects_3d}{SpatEnvelope,SpatEnvelope}(x, y, ...)

\S4method{intersects_3d}{SpatVoxel,SpatVoxel}(x, y, fun = function(v) !is.na(v))

\S4method{intersects_3d}{SpatEnvelope,SpatVoxel}(x, y, bounds = c("top", "midpoint"), ...)

\S4method{intersects_3d}{SpatVoxel,SpatEnvelope}(x, y, bounds = c("top", "midpoint"), ...)

\S4method{intersects_3d}{SpatEnvelope,ANY}(x, y, ...)

\S4method{intersects_3d}{SpatVoxel,ANY}(x, y, fun = function(v) !is.na(v))

\S4method{intersects_3d}{ANY,SpatEnvelope}(x, y, ...)

\S4method{intersects_3d}{ANY,SpatVoxel}(x, y, ...)

\S4method{intersects_3d}{ANY,ANY}(x, y, ...)
}
\arguments{
\item{x, y}{The two objects. At least one must be a \link[=SpatEnvelope-class]{SpatEnvelope} or
a \link[=SpatVoxel-class]{SpatVoxel}. The other can be either of those, a single-layer
\code{SpatRaster} footprint, or polygons (\code{SpatVector}, \code{sf}, \code{sfc}). Rasters
must share one grid (CRS, extent, resolution).}

\item{...}{Arguments for the voxel methods. They are an error when both
inputs are envelopes.}

\item{fun}{Voxel input only. A function of one depth layer that returns
\code{TRUE} where the voxel is occupied. Default: \code{function(v) !is.na(v)}.}

\item{bounds}{Only when one input is an envelope and the other a voxel. How
the envelope is placed on the voxel's depth levels: \code{"top"} (default) or
\code{"midpoint"}. See \code{\link[=envelope_to_voxel]{envelope_to_voxel()}}.}
}
\value{
A single-layer boolean \code{SpatRaster} named \code{intersects}, on the
grid of the 3D input: \code{TRUE}, \code{FALSE} or \code{NA} per cell as above.
}
\description{
\code{intersects_3d()} tests, cell by cell, whether \code{x} and \code{y} overlap in 3D.
It is the yes/no form of \code{\link[=intersect_3d]{intersect_3d()}} and takes the same inputs. It
computes only the presence pattern, with no cell areas and no volumes, so
it is the cheap choice for richness and tally maps.
}
\details{
Each cell gets one of three answers:
\itemize{
\item \code{TRUE}: both objects are present and their depths overlap.
\item \code{FALSE}: both are present but their depths do not overlap, or only
one of them is present.
\item \code{NA}: neither is present. There is nothing to compare.
}

This is how \code{terra} answers the 2D question for two rasters, so the result
sums and plots like any other boolean layer. Depth intervals that only touch
do not overlap. When \code{y} is a 2D footprint or polygons there is no depth
axis to compare, so the test is only whether both are present.

The order of \code{x} and \code{y} does not matter.
}
\examples{
fp <- terra::rast(nrows = 1, ncols = 4, xmin = 0, xmax = 4000,
                  ymin = 0, ymax = 1000,
                  crs = "+proj=laea +lat_0=0 +lon_0=0 +datum=WGS84 +units=m")

# Four cells. A is present in cells 1-3 at 0-100 m. B is present in cells
# 1, 2 and 4, at 50-200 m in cell 1 and 300-400 m elsewhere.
a <- as_envelope(terra::setValues(fp, c(1, 1, 1, NA)),
                 depth_min = 0, depth_max = 100)
b <- as_envelope(terra::setValues(fp, c(1, 1, NA, 1)),
                 depth_min = terra::setValues(fp, c(50, 300, NA, 300)),
                 depth_max = terra::setValues(fp, c(200, 400, NA, 400)))

# Cell 1: overlap. Cell 2: both present, depths disjoint. Cell 3: A only.
# Cell 4: B only.
terra::values(intersects_3d(a, b))

# Summing a stack of these gives a richness map. na.rm = TRUE counts each
# TRUE as 1 and each FALSE as 0.
richness <- sum(c(intersects_3d(a, b), intersects_3d(a, a)), na.rm = TRUE)
terra::values(richness)
}
\seealso{
\code{\link[=intersect_3d]{intersect_3d()}} for the shared space itself; \code{\link[terra:mask]{mask()}} to keep a
voxel's values inside a domain.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intersect.R
\name{mask-3d}
\alias{mask-3d}
\alias{mask,SpatVoxel,SpatEnvelope-method}
\alias{mask,SpatVoxel,SpatVoxel-method}
\alias{mask,SpatEnvelope,SpatEnvelope-method}
\alias{mask,SpatEnvelope,SpatVoxel-method}
\alias{mask,SpatRaster,SpatEnvelope-method}
\alias{mask,SpatRaster,SpatVoxel-method}
\title{Keep a raster's values inside a 3D domain}
\usage{
\S4method{mask}{SpatVoxel,SpatEnvelope}(x, mask, bounds = c("top", "midpoint"), ...)

\S4method{mask}{SpatVoxel,SpatVoxel}(x, mask, ...)

\S4method{mask}{SpatEnvelope,SpatEnvelope}(x, mask, ...)

\S4method{mask}{SpatEnvelope,SpatVoxel}(x, mask, bounds = c("top", "midpoint"), ...)

\S4method{mask}{SpatRaster,SpatEnvelope}(x, mask, ...)

\S4method{mask}{SpatRaster,SpatVoxel}(x, mask, ...)
}
\arguments{
\item{x}{The raster whose values are kept: a \link[=SpatVoxel-class]{SpatVoxel}, a
\link[=SpatEnvelope-class]{SpatEnvelope}, or a plain \code{SpatRaster}.}

\item{mask}{The 3D domain to keep \code{x} inside: a \link[=SpatEnvelope-class]{SpatEnvelope} or a
\link[=SpatVoxel-class]{SpatVoxel} on the same grid as \code{x}.}

\item{bounds}{Only when one side is an envelope and the other a voxel. How
the envelope is placed on the voxel's depth levels: \code{"top"} (default) or
\code{"midpoint"}. See \code{\link[=envelope_to_voxel]{envelope_to_voxel()}}.}

\item{...}{Passed on to \code{\link[terra:mask]{terra::mask()}}. For example, \code{inverse = TRUE}
keeps the values \emph{outside} the domain instead.}
}
\value{
\code{x}, with the same class and layers, with every value outside the
domain set to \code{NA}.
}
\description{
These methods make \code{\link[terra:mask]{terra::mask()}} depth-aware. \code{mask(x, mask)} keeps the
values of \code{x} where \code{mask} is present and sets the rest to \code{NA}. When
\code{mask} is a \link[=SpatEnvelope-class]{SpatEnvelope} or a \link[=SpatVoxel-class]{SpatVoxel}, "present" is
decided cell by cell \strong{and} depth by depth. A value of \code{x} stays only
where the mask domain reaches that cell at that depth.
}
\details{
What each pairing does:
\describe{
\item{voxel masked by an envelope}{The envelope is first placed on the
voxel's own depth levels with \code{\link[=envelope_to_voxel]{envelope_to_voxel()}}. Each depth layer
of the voxel is then masked by the matching level. This replaces the
hand-written \code{terra::mask(v, envelope_to_voxel(e, depths(v)))} and
cannot be pointed at the wrong depths.}
\item{voxel masked by a voxel}{Both must be sampled at the same depth
levels. Each depth layer of \code{x} is masked by the matching layer of the
mask, wherever the mask is occupied.}
\item{envelope masked by an envelope or a voxel}{A cell of \code{x} is kept
where \code{\link[=intersects_3d]{intersects_3d()}} finds the two domains overlap. Its depth
interval is kept whole. To narrow the interval instead, use
\code{\link[=intersect_3d]{intersect_3d()}}.}
\item{plain \code{SpatRaster} masked by an envelope or a voxel}{A cell is kept
where the mask domain is present at any depth. Without this method
terra would read the mask's first layer only.}
}

A voxel or envelope masked by a plain \code{SpatRaster} or by polygons is
terra's own \code{mask()}. Every layer is masked by the same 2D pattern and the
class is kept. No method is added for those cases.
}
\examples{
fp <- terra::rast(nrows = 1, ncols = 3, xmin = 0, xmax = 3000,
                  ymin = 0, ymax = 1000,
                  crs = "+proj=laea +lat_0=0 +lon_0=0 +datum=WGS84 +units=m")
terra::values(fp) <- c(1, 1, 1)

# A temperature voxel on three levels, and a species found at 0-100 m in
# cells 1-2 only.
temp <- as_voxel(terra::rast(list(terra::setValues(fp, c(20, 21, 22)),
                                  terra::setValues(fp, c(15, 16, 17)),
                                  terra::setValues(fp, c(10, 11, 12)))),
                 depths = c(0, 100, 200), varname = "temp")
range_env <- as_envelope(terra::setValues(fp, c(1, 1, NA)),
                         depth_min = 0, depth_max = 100)

# The temperatures inside the species' range: cell 3 and the 200 m level
# are gone.
terra::values(mask(temp, range_env))

# A 2D raster masked by the range keeps cells where the range is present
# at any depth.
terra::values(mask(fp, range_env))
}
\seealso{
\code{\link[=intersect_3d]{intersect_3d()}} for the shared domain itself; \code{\link[=intersects_3d]{intersects_3d()}}
for the yes/no test; \code{\link[=extract_to_area]{extract_to_area()}} to restrict a voxel to a polygon
and one depth band instead of a per-cell depth window.
}

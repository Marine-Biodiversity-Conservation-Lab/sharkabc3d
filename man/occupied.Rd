% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_3d_objects.R
\name{occupied}
\alias{occupied}
\title{Reduce a variable voxel to a presence voxel}
\usage{
occupied(x, fun = function(v) !is.na(v))
}
\arguments{
\item{x}{A \link[=SpatVoxel-class]{SpatVoxel}.}

\item{fun}{Function taking a vector of cell values and returning a logical
vector of the same length. \code{NA} counts as \code{FALSE}. Defaults to
\code{function(v) !is.na(v)}, which treats any recorded value as occupied.}
}
\value{
A \link[=SpatVoxel-class]{SpatVoxel} on the depth levels of \code{x}. Values are 1
(occupied) or \code{NA}. Layers are named \code{presence_depth={value}}.
}
\description{
A \link[=SpatVoxel-class]{SpatVoxel} holds a variable, such as temperature, oxygen or
fishing effort. It does not say which cells count as occupied.
\code{occupied()} applies a predicate and returns presence: 1 where \code{fun} is
\code{TRUE}, \code{NA} elsewhere. The grid and the depth levels stay the same.
}
\details{
The 3D query verbs read presence directly and take no predicate. Threshold
each voxel first, so both sides of a query use their own cutoff:

\if{html}{\out{<div class="sourceCode">}}\preformatted{intersect_3d(occupied(temp, function(v) v > 15),
             occupied(effort, function(v) v > 0))
}\if{html}{\out{</div>}}

\code{fun} receives one depth layer's cell values as a numeric vector. It must
return a logical vector of the same length. A predicate that summarises the
layer, such as \code{function(v) v > mean(v, na.rm = TRUE)}, therefore compares
each cell against its own depth. \code{\link[=voxel_to_envelope]{voxel_to_envelope()}} uses the same
contract.

Running \code{occupied()} on a presence voxel returns it unchanged.
}
\examples{
r <- terra::rast(nrows = 1, ncols = 3, nlyrs = 2)
terra::values(r) <- cbind(c(12, 18, 4), c(9, 16, 2))
v <- as_voxel(r, depths = c(0, 100), varname = "temp")

# Occupied where the water is warmer than 10 degrees.
warm <- occupied(v, function(x) x > 10)
names(warm)
terra::values(warm)

# The verbs need no predicate.
volume(warm)

# A presence voxel is returned unchanged.
identical(terra::values(occupied(warm)), terra::values(warm))

# Each side of a query uses its own cutoff.
effort <- as_voxel(terra::setValues(r, cbind(c(0, 3, 0), c(0, 2, 0))),
                   depths = c(0, 100), varname = "effort")
terra::values(intersect_3d(warm, occupied(effort, function(x) x > 0)))
}
\seealso{
\code{\link[=voxel_to_envelope]{voxel_to_envelope()}} collapses the result to a
\link[=SpatEnvelope-class]{SpatEnvelope}. \code{\link[=volume]{volume()}}, \code{\link[=intersect_3d]{intersect_3d()}}, \code{\link[=intersects_3d]{intersects_3d()}} and
\code{\link[terra:mask]{mask()}} take a presence voxel.
}

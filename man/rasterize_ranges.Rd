% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volume.R
\name{rasterize_ranges}
\alias{rasterize_ranges}
\title{Rasterize multiple ranges onto a study grid}
\usage{
rasterize_ranges(
  sf_data,
  grid,
  bathymetry,
  depth_min_col,
  depth_max_col,
  name_col = NULL
)
}
\arguments{
\item{sf_data}{sf object. Each row is a separate range to rasterize.}

\item{grid}{SpatRaster. Study area raster grid (e.g., from
\code{\link[=create_study_raster]{create_study_raster()}}).}

\item{bathymetry}{SpatRaster. Bathymetry raster with negative values for
depth below sea level (e.g., from \code{\link[=load_bathymetry]{load_bathymetry()}}).}

\item{depth_min_col}{Character. Column name in \code{sf_data} containing the
minimum (shallowest) depth in metres.}

\item{depth_max_col}{Character. Column name in \code{sf_data} containing the
maximum (deepest) depth in metres.}

\item{name_col}{Character. Optional column name to use for naming the
output list. Default \code{NULL} (unnamed).}
}
\value{
Named list of multi-layer SpatRasters (output of
\code{\link[=rasterize_range]{rasterize_range()}}).
}
\description{
Wrapper around \code{\link[=rasterize_range]{rasterize_range()}} that processes multiple rows of an sf
object, each with its own depth limits. Displays a progress bar.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise.R
\name{temporal_summarise}
\alias{temporal_summarise}
\title{Summarise across temporal dimension}
\usage{
temporal_summarise(
  file_paths,
  fun = c("mean", "min", "max", "sd"),
  start_datetime = NULL,
  end_datetime = NULL,
  output_dir = NULL,
  filename = NULL,
  na.rm = TRUE,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{file_paths}{Character vector. Paths to one or more Copernicus netCDF
(\code{.nc} or \code{.nc4}) files.}

\item{fun}{Character vector of temporal summary statistics. Supported values
are \code{"mean"}, \code{"min"}, \code{"max"}, and \code{"sd"}.}

\item{start_datetime}{Optional start datetime used to filter the available
netCDF time steps before summarising. A \code{Date}, \code{POSIXt}, or character
value interpreted in UTC.}

\item{end_datetime}{Optional end datetime. Defaults to \code{start_datetime} when
only a start is supplied. If both are \code{NULL}, all available time steps are
summarised.}

\item{output_dir}{Optional character. Directory in which summarised netCDF
files are written. Defaults to the directory containing the first input
file.}

\item{filename}{Optional character. Output filename. When multiple statistics
are requested, the statistic is appended before the extension
(for example \code{summary_mean.nc}, \code{summary_max.nc}).}

\item{na.rm}{Logical. If \code{TRUE} (default), missing values are ignored within
each grid cell/depth combination. If \code{FALSE}, any missing temporal value
produces a missing summary value at that location.}

\item{force}{Logical. Overwrite existing summary files. Default \code{FALSE}.}

\item{quiet}{Logical. Suppress progress messages. Default \code{FALSE}.}
}
\value{
Named character vector containing paths to the summarised netCDF
files, with names corresponding to \code{fun}.
}
\description{
Summarise multiple netCDF files across their temporal
dimension while preserving all non-temporal dimensions (for example
longitude, latitude, depth, or projected x/y dimensions).
}

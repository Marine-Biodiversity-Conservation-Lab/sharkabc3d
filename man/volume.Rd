% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/volume.R
\name{volume}
\alias{volume}
\alias{volume,SpatEnvelope-method}
\alias{volume,SpatVoxel-method}
\alias{volume,SpatRaster-method}
\title{Total 3D volume of a rasterized domain}
\usage{
volume(x, ...)

\S4method{volume}{SpatEnvelope}(x, ...)

\S4method{volume}{SpatVoxel}(x, bounds = c("top", "midpoint"), fun = function(v) !is.na(v))

\S4method{volume}{SpatRaster}(x, ...)
}
\arguments{
\item{x}{A \link[=SpatEnvelope-class]{SpatEnvelope} or \link[=SpatVoxel-class]{SpatVoxel}. A bare SpatRaster is
rejected; build one with \code{\link[=as_envelope]{as_envelope()}}, \code{\link[=vect_to_envelope]{vect_to_envelope()}} or
\code{\link[=as_voxel]{as_voxel()}} first.}

\item{...}{Arguments for the \link[=SpatVoxel-class]{SpatVoxel} method, which are an error
for an envelope.}

\item{bounds}{SpatVoxel only. What each depth level stands for vertically:
\code{"top"} (default) treats the level as the top of its slab, so the slab
runs down to the next level; \code{"midpoint"} puts the level in the middle,
with edges halfway to each neighbour (the World Ocean Atlas convention).
Under \code{"top"} the deepest level has no next level and contributes no
volume. Outer edges are clamped to \code{range(depths)} under both. Same
argument, same meaning, as in \code{\link[=envelope_to_voxel]{envelope_to_voxel()}}.}

\item{fun}{SpatVoxel only. Predicate deciding whether a voxel is occupied,
applied one depth layer at a time. Defaults to \code{function(v) !is.na(v)},
the same default \code{\link[=voxel_to_envelope]{voxel_to_envelope()}} uses. \code{NA} results count as
unoccupied.}
}
\value{
Numeric of length 1. Total volume in km³.
}
\description{
Sums the occupied volume of a \link[=SpatEnvelope-class]{SpatEnvelope} or a \link[=SpatVoxel-class]{SpatVoxel}
over the whole grid. Both representations describe a 3D domain over a 2D
grid, but they store depth in dual roles, so each computes the vertical
extent differently:
}
\details{
\describe{
\item{\link[=SpatEnvelope-class]{SpatEnvelope}}{Depth is the cell value, and the interval is
solid by construction. Volume is
\code{sum(cell_area * (depth_max - depth_min))} over present cells.}
\item{\link[=SpatVoxel-class]{SpatVoxel}}{Depth is the layer index. Each occupied voxel
contributes its cell area times the thickness of the slab its depth
level stands for, so interior gaps are excluded rather than filled.}
}

A voxel volume is quantized to the levels the voxel was sampled at, and a
voxel built by \code{\link[=envelope_to_voxel]{envelope_to_voxel()}} counts a level as occupied on \emph{any}
overlap with the envelope. So a range of 0-100 m discretized onto
0, 50, 100, 150, 200 m occupies three levels spanning 0-150 m and reports
more water than the envelope it came from. Coarse levels overstate; the
error shrinks as the levels tighten around the range. Where an envelope's
limits fall exactly on levels under \code{bounds = "top"}, the two agree.
}
\examples{
fp <- terra::rast(nrows = 2, ncols = 2, xmin = 0, xmax = 2000,
                  ymin = 0, ymax = 2000,
                  crs = "+proj=laea +lat_0=0 +lon_0=0 +datum=WGS84 +units=m")
terra::values(fp) <- c(1, 1, 1, NA)

# 2.5D: three 1 km² cells, each 0-200 m => 3 * 1 * 0.2 = 0.6 km³.
e <- as_envelope(fp, depth_min = 0, depth_max = 200)
volume(e)

# 3D: the same domain on three levels. Under "top" the slabs are 100 m,
# 100 m and 0 m, so the discretized volume matches exactly here.
v <- envelope_to_voxel(e, depths = c(0, 100, 200))
volume(v)

# An interior gap is volume a voxel can drop and an envelope cannot.
gappy <- v
gappy[[2]] <- terra::setValues(terra::rast(v[[2]]), NA)
gappy <- as_voxel(gappy)
volume(gappy)
volume(voxel_to_envelope(gappy))
}
\seealso{
\code{\link[=calc_volume_overlap]{calc_volume_overlap()}} for the volume two domains share;
\link[=SpatVolume-class]{SpatVolume} for why the two representations dispatch separately.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/voxel_profiles.R
\name{voxel_profiles}
\alias{voxel_profiles}
\alias{profile_flat}
\alias{profile_equal}
\title{Vertical profiles for voxel building}
\usage{
profile_flat(ind, depths, n_depths)

profile_equal(ind, depths, n_depths)
}
\arguments{
\item{ind}{Logical SpatRaster of occupancy, one layer per depth.}

\item{depths}{Numeric vector of depths, shallow to deep, one per layer of
\code{ind}.}

\item{n_depths}{Single-layer SpatRaster counting each cell's occupied levels.}
}
\value{
A weight to multiply \code{ind} by: a single number, or a SpatRaster on
the grid of \code{ind} with either one layer or one per depth.
}
\description{
A vertical profile decides how the value a cell carries is spread down the
depth levels that cell occupies. \code{\link[=envelope_to_voxel]{envelope_to_voxel()}} takes one through its
\code{profile} argument, as a function — either one of these, or one you write.
}
\section{Functions}{
\itemize{
\item \code{profile_flat()}: The whole value at every occupied level. This is
what \code{profile = NULL} means in \code{\link[=envelope_to_voxel]{envelope_to_voxel()}}; it exists as a
function so the no-profile case is a profile like any other rather than a
special case in the caller.

\item \code{profile_equal()}: An even share at each occupied level, so the
levels sum back to the cell's value. The share depends on how many levels
the cell occupies, which varies across the grid wherever the envelope
does: a cell spanning three levels gets a third at each, one spanning ten
gets a tenth.

}}
\section{Writing a profile}{


A profile is called once per \code{\link[=envelope_to_voxel]{envelope_to_voxel()}} call, with three
arguments, positionally:

\describe{
\item{\code{ind}}{Logical SpatRaster, one layer per depth, \code{TRUE} where a
cell's envelope contains that depth. Cells that are \code{NA} in the envelope
are \code{NA} here.}
\item{\code{depths}}{Numeric vector of the depths those layers stand for,
shallow to deep. Always \code{terra::nlyr(ind)} long.}
\item{\code{n_depths}}{Single-layer SpatRaster counting the levels each cell
occupies, i.e. \code{sum(ind)}. Passed in rather than left to be recomputed,
since the caller already needs it.}
}

It returns the weight that multiplies \code{ind}, in one of three forms: a single
number applying to the whole grid, a single-layer SpatRaster giving one
weight per cell, or a \code{terra::nlyr(ind)}-layer SpatRaster giving one per cell
per level. Rasters must be on the grid of \code{ind}.

Because a profile is handed rasters and returns rasters, write it in terra
algebra rather than in terms of one cell at a time. Unoccupied cells are
masked out by the caller afterwards, so a profile need not zero them — but
it should avoid dividing by a zero level count, since \code{Inf} is harder to read
in a partial result than \code{NA}.

A profile that conserves does so \emph{per cell}: the weights it returns sum to 1
within each cell, whatever that cell's interval.
}

\examples{
fp <- terra::rast(nrows = 1, ncols = 2, xmin = 0, xmax = 2, ymin = 0, ymax = 1)
terra::values(fp) <- c(1, 1)
dmax <- terra::setValues(terra::rast(fp), c(100, 300))
e <- as_envelope(fp, depth_min = 0, depth_max = dmax)
depths <- c(0, 100, 200, 300)

# the whole value at every occupied level
terra::values(envelope_to_voxel(e, depths, values = 12, profile = profile_flat))

# an even share instead, so each cell's levels sum back to its value
terra::values(envelope_to_voxel(e, depths, values = 12, profile = profile_equal))

# a profile of your own: weight the shallow levels twice as heavily as the
# deep ones, still conserving each cell's total
surface_weighted <- function(ind, depths, n_depths) {
  w <- terra::rast(lapply(depths, function(d) ifelse(d <= 100, 2, 1) * ind[[1]]))
  w <- w * ind
  w / sum(w)
}
terra::values(envelope_to_voxel(e, depths, values = 12,
                                profile = surface_weighted))
}

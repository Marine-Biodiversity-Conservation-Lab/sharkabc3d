% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_3d_objects.R
\name{voxel_to_envelope}
\alias{voxel_to_envelope}
\title{Collapse Voxel 3D -> Envelope 2.5D}
\usage{
voxel_to_envelope(v, fun = function(x) !is.na(x))
}
\arguments{
\item{v}{SpatVoxel (or a multi-depth SpatRaster with
\verb{\{variable\}_depth=\{value\}} layer names).}

\item{fun}{Function taking a vector of cell values and returning a logical
vector of the same length. \code{NA} results are treated as \code{FALSE}. Defaults to
\verb{\\(x) !is.na(x)}.}
}
\value{
A \linkS4class{SpatEnvelope} with layers \code{depth_min} and \code{depth_max}, on
the same grid as \code{v}.
}
\description{
Reduce a \linkS4class{SpatVoxel} to the \linkS4class{SpatEnvelope} that bounds it. A
predicate \code{fun} is applied to the cell values at each depth; for every cell,
the shallowest depth at which the predicate is \code{TRUE} becomes \code{depth_min}
and the deepest becomes \code{depth_max}. Cells where the predicate is never
\code{TRUE} are \code{NA} in both layers.
}
\details{
The default predicate, \verb{\\(x) !is.na(x)}, gives the plain vertical extent of
the data: the shallowest and deepest depths at which the cell has any value.
Pass a different predicate to bound a subset of the values instead, e.g.
\verb{\\(x) x > 15} for the depths over which a cell exceeds 15 degrees.

\strong{This conversion is lossy.} An envelope stores a single continuous
interval per cell, so any interior gap in the voxel is filled in: a cell that
satisfies \code{fun} at 0 m and 200 m but not at 100 m still yields the envelope
\verb{[0, 200]}. Use \linkS4class{SpatVoxel} directly where interior gaps matter.
}
\examples{
# Two cells sampled at four standard depths.
r <- terra::rast(nrows = 1, ncols = 2, xmin = 0, xmax = 2, ymin = 0, ymax = 1,
                 nlyrs = 4)
terra::values(r) <- cbind(c(12, NA), c(11, NA), c(NA, 8), c(6, NA))
v <- as_voxel(r, depths = c(0, 50, 100, 200), varname = "temp")

# Default predicate: the vertical extent of the data. Cell 1 has values at
# 0, 50 and 200 m, so it comes back as [0, 200] — the gap at 100 m is filled,
# because an envelope stores one continuous interval per cell.
terra::values(voxel_to_envelope(v))

# Another predicate bounds a subset of the values instead: here the depths
# over which a cell is warmer than 10 degrees. Cell 2 never qualifies, so it
# is NA in both layers.
terra::values(voxel_to_envelope(v, fun = function(x) x > 10))
}
\seealso{
\code{\link[=envelope_to_voxel]{envelope_to_voxel()}}, the reverse expansion.
}

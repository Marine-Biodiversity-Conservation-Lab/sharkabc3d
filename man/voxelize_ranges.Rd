% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_3d_objects.R
\name{voxelize_ranges}
\alias{voxelize_ranges}
\title{Voxelize multiple ranges onto a study grid}
\usage{
voxelize_ranges(
  sf_data,
  voxel,
  bathymetry = NULL,
  depth_min_col,
  depth_max_col,
  name_col = NULL
)
}
\arguments{
\item{sf_data}{sf object. Each row is a separate range to rasterize.}

\item{voxel}{The voxel grid that defines the study area. Either a
\code{study_voxel} object (from \code{\link[=create_study_voxel]{create_study_voxel()}}) or a plain SpatRaster
template (e.g., from \code{\link[=create_study_raster]{create_study_raster()}}) whose cells become the
horizontal footprint of each voxel column.}

\item{bathymetry}{SpatRaster. Seafloor depth raster (positive values in
metres) matching \code{voxel}. Optional and ignored when \code{voxel} is a
\code{study_voxel} (its seafloor is used).}

\item{depth_min_col}{Character. Column name in \code{sf_data} containing the
minimum (shallowest) depth in metres.}

\item{depth_max_col}{Character. Column name in \code{sf_data} containing the
maximum (deepest) depth in metres.}

\item{name_col}{Character. Optional column name to use for naming the
output list. Default \code{NULL} (unnamed).}
}
\value{
Named list of multi-layer SpatRasters (output of
\code{\link[=voxelize_range]{voxelize_range()}}).
}
\description{
Wrapper around \code{\link[=voxelize_range]{voxelize_range()}} that processes multiple rows of an sf
object, each with its own depth limits. Displays a progress bar.
}

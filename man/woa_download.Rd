% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/woa.R
\name{woa_download}
\alias{woa_download}
\title{Download a WOA NetCDF file (with caching)}
\usage{
woa_download(
  variable,
  period = "annual",
  resolution = "0.25",
  decade = NULL,
  output_dir = NULL,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{variable}{Character. One of \code{"temperature"}, \code{"salinity"},
\code{"dissolved_oxygen"}, \code{"oxygen_saturation"}, \code{"AOU"}, \code{"nitrate"},
\code{"phosphate"}, \code{"silicate"}, \code{"density"}.}

\item{period}{Character or numeric. \code{"annual"} (default), \code{"monthly"}
(all 12 months), \code{"seasonal"} (4 seasons), or a numeric vector where 0 =
annual, 1:12 = monthly, 13:16 = seasonal.}

\item{resolution}{Character or numeric. \code{"0.25"} (default), \code{"1"}, or \code{"5"}
degrees.}

\item{decade}{Character. Decade code (e.g. \code{"decav"}, \code{"all"}). Defaults to
the canonical decade for the variable.}

\item{output_dir}{Character. Destination directory. Defaults to
\code{\link[=woa_cache_dir]{woa_cache_dir()}}.}

\item{force}{Logical. If \code{TRUE}, re-download even if the file exists.
Default \code{FALSE}.}

\item{quiet}{Logical. Suppress download progress. Default \code{FALSE}.}
}
\value{
Character vector of paths to downloaded .nc files.
}
\description{
Download World Ocean Atlas 2023 NetCDF files from the NCEI THREDDS server.
Files are cached in \code{\link[=woa_cache_dir]{woa_cache_dir()}} (or a user-supplied \code{output_dir}) and
skipped on subsequent calls unless \code{force = TRUE}.
}
\details{
On first use of the persistent cache in an interactive session, the function
prompts for consent to write to user filespace (per CRAN Repository Policy).
When the \code{curl} package is available, the estimated size of each remote
file is reported before downloading. A full set of WOA files can be many
gigabytes — supply \code{output_dir} to direct the cache elsewhere (HPC scratch,
external drive, etc.).
}
